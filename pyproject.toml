[project]
name = "nt-quant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cvxpy>=1.8.0",
    "dataframely>=2.6.0",
    "numpy>=2.4.1",
    "polars>=1.37.1",
]

[dependency-groups]
dev = [
    "bear-lake>=0.1.5",
    "python-dotenv>=1.2.1",
]
